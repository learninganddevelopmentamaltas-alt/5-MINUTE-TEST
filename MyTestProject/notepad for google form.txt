<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>5 Minute Test</title>
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            /* REPLACE 'your-background-image.jpg' WITH YOUR ACTUAL IMAGE LINK OR FILE PATH */
            background: linear-gradient(rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.6)), url('background.jpg') no-repeat center center fixed;
            background-size: cover;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            color: #333;
        }

        /* Fixed Header for Timer */
        .header-timer {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            background-color: rgba(0, 0, 0, 0.85);
            color: #fff;
            text-align: center;
            padding: 15px;
            font-size: 1.5rem;
            font-weight: bold;
            z-index: 1000;
            box-shadow: 0 4px 10px rgba(0,0,0,0.3);
            letter-spacing: 1px;
        }

        .header-timer span {
            color: #ff4d4d;
        }

        /* Container for the Google Form */
        .form-container {
            margin-top: 90px; /* Space so the sticky timer doesn't block the form */
            margin-bottom: 30px;
            width: 95%;
            max-width: 800px;
            background: rgba(255, 255, 255, 0.95);
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.3);
            padding: 10px;
        }

        /* Responsive Iframe Container */
        .iframe-wrapper {
            position: relative;
            width: 100%;
            height: 80vh; /* Utilizes most of the screen height dynamically */
            -webkit-overflow-scrolling: touch;
            overflow-y: auto;
        }

        iframe {
            width: 100%;
            height: 100%;
            border: none;
        }

        /* Overlay shown when time expires */
        .timeout-overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.95);
            color: white;
            z-index: 9999;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            text-align: center;
            padding: 20px;
        }

        .timeout-overlay h1 {
            font-size: 2.5rem;
            margin-bottom: 15px;
            color: #ff4d4d;
        }

        .timeout-overlay p {
            font-size: 1.2rem;
        }

        /* Mobile Adjustments */
        @media (max-width: 600px) {
            .header-timer {
                font-size: 1.2rem;
                padding: 12px;
            }
            .form-container {
                margin-top: 75px;
                width: 98%;
                padding: 5px;
            }
            .iframe-wrapper {
                height: 78vh;
            }
        }
    </style>
</head>
<body>

    <div class="header-timer">
        5 MINUTE TEST — TIME REMAINING: <span id="timer">05:00</span>
    </div>

    <div class="form-container">
        <div class="iframe-wrapper">
            <iframe id="quiz-iframe" src="https://docs.google.com/forms/d/e/1FAIpQLSePJNpjPwfaDGZFCeoNXwQJinzgyn7RSdKVCuj2E0foRE0yeQ/viewform?embedded=true">
                Loading…
            </iframe>
        </div>
    </div>

    <div class="timeout-overlay" id="timeoutScreen">
        <h1>Time is Up!</h1>
        <p>The 5 minutes allocated for this test have expired.</p>
    </div>

    <script>
        // Set test duration in seconds (5 minutes = 300 seconds)
        let timeRemaining = 300; 
        const timerElement = document.getElementById('timer');
        const timeoutScreen = document.getElementById('timeoutScreen');
        const quizIframe = document.getElementById('quiz-iframe');

        const countdown = setInterval(() => {
            let minutes = Math.floor(timeRemaining / 60);
            let seconds = timeRemaining % 60;

            // Format numbers to always show two digits
            minutes = minutes < 10 ? '0' + minutes : minutes;
            seconds = seconds < 10 ? '0' + seconds : seconds;

            timerElement.textContent = `${minutes}:${seconds}`;

            if (timeRemaining <= 0) {
                clearInterval(countdown);
                // Hide the test and show the time-out screen
                timeoutScreen.style.display = 'flex';
                quizIframe.remove(); // Removes the form so they cannot continue typing
            }

            timeRemaining--;
        }, 1000);
    </script>

</body>
</html>